import nameCategory from "../components/SideBar"

export const ROUTES = {
    HOME : '/',
    CART : '/cart',
    PROFILE: '/profile',
    // CATEGORY: '/categories/:id',
    CATEGORY: `/products/category/:title`,
    PRODUCT: '/products/:id',
}
